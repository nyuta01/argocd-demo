name: release-please
on:
  push:
    branches:
      - main
jobs:
  release-please:
    runs-on: ubuntu-latest
    steps:
      - name: release-please-action
        id: release
        uses: GoogleCloudPlatform/release-please-action@v2
        with:
          release-type: simple
          
